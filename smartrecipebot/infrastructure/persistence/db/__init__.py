"""
Database domain package.

Contains SQLAlchemy ORM models and related helpers. Import `smartrecipebot.infrastructure.persistence.db.models`
for the concrete table definitions instead of the older `smartrecipebot.domain.models.*`
modules.
"""

